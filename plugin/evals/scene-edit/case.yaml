schema_version: "1.1"
name: scene-edit
description: 기존 덱의 한 장면을 다른 장면 타입으로 바꾸는 회귀 시험(P7 evals.json 케이스 3 이식)
tags: [smoke]
context:
  # 빈 작업 폴더에 topics/_sample을 만든다. `--scaffold`를 줄 때만 실행된다.
  scaffold_script: fixture.sh
